# 2026-03-31 Daily

## 実施内容

- `mvp/` 配下にビジュアル回帰テスト基盤を追加
  - `package.json` を追加（`@playwright/test` を devDependencies に追加）
  - `playwright.config.js` を追加
  - `test_server.js` を追加
  - `tests/visual/viewer.visual.spec.js` を追加
- `mvp/README.md` に visual regression tests の手順を追記

## 依存関係メモ

- 追加: `@playwright/test` (`devDependencies`)
- 実行済み:
  - `npm install`
  - `npx playwright install chromium`

## 確認

- `npx playwright install chromium` 完了
- ビジュアルテスト本実行は次サイクルで実施予定