# 2026-04-02 Daily

## 実施内容

- `beta/viewer.html`
  - `board` を app 直下へ移し、canvas を viewer の全面ベースとして配置
  - toolbar / meta / shortcuts / status を `overlay-ui` 配下へ移し、canvas 上に浮く UI 構成へ変更
- `beta/viewer.css`
  - `.app` を全面キャンバスのコンテナに変更
  - `.board` を viewport 全面に広げ、toolbar / meta を半透明の floating panel として重ねるレイアウトへ変更
  - mobile 幅でも overlay panel が収まるように最大幅を調整

## 確認

- `npm --prefix beta run build:browser`
  - 結果: pass
- `npm --prefix beta run test:unit`
  - 結果: pass

## 次の一手

- fit-all の余白計算を実描画基準で見直し、外周が欠けないか visual test で固定する

- `beta/src/browser/viewer.ts`
  - collapse 状態ノードの右側に、小さな補助ノード付きの count badge を描画
  - collapsed subtree の子孫数を集計して badge 数字に反映
  - badge クリック時は drag に入らず、その場で expand するように変更
- `beta/viewer.css`
  - collapsed count badge のスタイルを追加

## 確認

- `npm --prefix beta run build:browser`
  - 結果: pass
- `npm --prefix beta run test:unit`
  - 結果: pass

## 次の一手

- collapsed badge の hover/selected 状態を整えつつ、graph-level `Link` overlay 描画へ進む

## 追加実施（Scope 移動）

- `beta/viewer.html`
  - `Enter scope` / `Back scope` ボタンを追加
  - scope 移動ショートカット表示を追加
- `beta/src/browser/viewer.globals.d.ts`
  - `ViewState.currentScopeRootId` を追加
- `beta/src/browser/viewer.ts`
  - `currentScopeRootId` を表示 root として扱う処理を追加
  - folder ノードを scope 入口として扱う `enterScope` / `exitScope` を追加
  - toolbar と `Ctrl/Cmd+]` / `Ctrl/Cmd+[` で scope 移動できるようにした
  - `scope: root / ...` の breadcrumb 風メタ表示を追加
  - layout / render を current scope 起点へ変更

## 確認（Scope 移動）

- `npm --prefix beta run build:browser`
  - 結果: pass
- `npm --prefix beta run test:unit`
  - 結果: pass

## 次の一手（Scope 移動）

- folder type の作成導線と、scope 外ノードの要約表示を viewer に追加する

## 追加実施（Folder / Scope Summary / Alias）

- `beta/viewer.html`
  - `Make folder` / `Add alias` / `Jump target` ボタンを追加
  - `scope-summary` 行を追加
- `beta/src/browser/viewer.ts`
  - selected node を `folder` 化する `makeSelectedFolder` を追加
  - folder ノードを current scope では scope 入口 leaf として扱うように変更
  - current scope の外側要約として
    - parent scope
    - child scope 一覧と省略ノード数
    を `scope-summary` に表示
  - current scope root 配下へ alias を追加する `addAliasInCurrentScope` を追加
  - alias 選択時に target 実体の scope へ移動する `jumpToAliasTarget` を追加
  - folder ノードに `scope` badge を表示
- `beta/viewer.css`
  - `scope` badge の色を追加

## 確認（Folder / Scope Summary / Alias）

- `npm --prefix beta run build:browser`
  - 結果: pass
- `npm --prefix beta run test:unit`
  - 結果: pass

## 次の一手（Folder / Scope Summary / Alias）

- graph-level `Link` の overlay 描画と、cross-scope 表示ポリシーを viewer に入れる

## 追加実施（Flash / Rapid / Deep UI）

- `beta/viewer.html`
  - toolbar に `Flash` / `Rapid` / `Deep` の mode switch を追加
  - `mode: ...` の meta 表示と `1/2/3` ショートカット案内を追加
- `beta/viewer.css`
  - mode switch の group / active 状態スタイルを追加
- `beta/src/browser/viewer.globals.d.ts`
  - `ThinkingMode` 型と `ViewState.thinkingMode` を追加
- `beta/src/browser/viewer.ts`
  - viewer 非永続 state として `thinkingMode` を追加
  - toolbar ボタンと `1/2/3` キーで `Flash` / `Rapid` / `Deep` を切り替えられるように変更
  - active button と `mode: ...` meta を同期する処理を追加
  - document load 時は `Rapid` を既定 mode に戻すようにした

## 確認（Flash / Rapid / Deep UI）

- `npm --prefix beta run build:browser`
  - 結果: pass
- `npm --prefix beta run test:unit`
  - 結果: pass

## 次の一手（Flash / Rapid / Deep UI）

- mode ごとの実際の操作差分を定義するか、単なる viewer profile 切替に留めるかを仕様で決める
