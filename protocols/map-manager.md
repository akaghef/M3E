# Map Manager Protocol

This file is a compatibility entry point.

The operational SSOT is the package:

```text
protocols/map-manager/
```

Start with:

- `protocols/map-manager/README.md`
- `protocols/map-manager/gates.md`
- `protocols/map-manager/projection-rule.md`

Do not add new Map Manager rules to this wrapper. Update the package and the
machine-checkable contracts under `protocols/contracts/` instead.
