# 260502

## M3E path notation

- viewer のコピー用パス表記を `M:(map)> A > B >> C` 形式へ変更した。
- `>>` は scope root に入る境界として出力する。
- `/api/maps/:mapId/resolve?path=...` 側も新表記を受け付けるようにし、旧 `Map:Root/A` 形式は維持した。
- 検証: `npm run build`, `npm run build:node; npx vitest run tests/unit/path_resolve.test.js`

## Executive A2A conversation pipeline

- `beta/system/a2a/agent-card.json` を追加し、M3E Executive の Agent Card を A2A v0.3.0 形で定義した。
- `beta/system/a2a-executive.mjs` を追加し、Secretary → Executive の依頼を `message/send` 風 request、Task snapshot、outbox status として保存できるようにした。
- 既存の slideshow / tour 依頼を `task-20260502015441-3mnnxi` として登録し、状態を `working` に更新した。
- 検証: `node --check beta/system/a2a-executive.mjs`, `node beta/system/a2a-executive.mjs card`, `send`, `reply`, `list`。
## Akaghef-hosted team ws / Supabase VM test

- 今日の S2/S3 実地検証目標として、Akaghef が host / owner になる team workspace を Supabase 経由で共有し、10人程度が同じ map を編集できるかを試す方針を確定。
- Decision Pool に `2026-05-02-006` を追加し、Supabase は正本ではなく hosted transport / backend、正本性・採否・復元責任は M3E 側に残すと記録。
- demo 用 SQL `docs/03_Spec/supabase_schema_team_demo.sql` を追加。`map_team_260502` だけを anon/auth client から read/write 可能にし、既存の広い development policy が残っている場合は削除する。
- VM / 複数 client 検証用 launcher `scripts/ops/launch-beta-team-supabase-client.bat` を追加。participant ごとに別 `M3E_DATA_DIR`、別 port、同一 Supabase map で起動できる。
- Akaghef 向け runbook `docs/for-akaghef/260502_team_ws_supabase_vm_test.md` を追加。host seed push、VM pull/push、10人 scope 分担、合格/中止条件を整理。
- `A,A,B,B,全部` の判断を Decision Pool `2026-05-02-007` に記録。Tailscale / VPN 内リンク、view/editor link 分離、assigned scope、auto-sync、合格ライン全部を今日の検証仕様とした。
- Journey 図 `docs/for-akaghef/260502_team_ws_journey.html` を追加。
- Playwright E2E `beta/tests/e2e/team_ws_vm.spec.js` と `beta/playwright.team-vm.config.js` を追加。host/vm 2プロセス、別 data dir、共有 file transport で seed push、VM edit、host pull、stale concurrent edit 409、conflict backup を検証。
- 検証: `npm --prefix beta run build` pass。`npx playwright test --config playwright.team-vm.config.js --retries 0 --workers 1` pass。`--repeat-each 5` で 10 tests pass。
- 既存 `scripts/ops/vm_test.bat` を参考に、team ws 用 VirtualBox runner `scripts/ops/vm_team_ws_test.bat` と VM 内 runner `scripts/ops/run_team_ws_test.bat` を追加。`build_test_package.bat` で team runner も package へコピーするようにした。
- Tailscale 補助 `scripts/ops/setup-tailscale-windows.ps1` を追加。MSI silent install と `TS_AUTH_KEY` 環境変数を使った tailnet join を想定。auth key 発行は Tailscale admin console 側で必要。
- 検証: `vm_team_ws_test.bat warm` は VM 側 Guest Additions not ready / not installed で実行前に停止。`setup-tailscale-windows.ps1 -SkipUp` は Tailscale 未インストールのため MSI 指定が必要と確認。
- VM 用 package を `scripts/ops/build_test_package.bat` で作成し、`C:\M3E_test_package\run_team_ws_test.bat` をローカル実行して Setup / Launch / Smoke PASS を確認。結果: `C:\M3E_test_reports\team_ws_20260502_121155\report.txt`。
- Secretary 向けに `beta/system/executive_outbox.md` へ key 発行は Akaghef 側、VM test 環境は package PASS、残 blocker は VirtualBox Guest Additions readiness として引き継ぎを追記。
- Journey 図 `docs/for-akaghef/260502_team_ws_journey.html` を成熟度マップに詳細化。Verified / Ready / Blocked / Missing / Planned の色分けで Host、Network、Supabase、VM、Team operation、Acceptance を分解。Journey smoke test も新ラベルへ更新し pass。
- Tailscale 以外の未成熟点を処理。`access=view` / `access=edit` による view-only / editor link 分離を Beta viewer に実装し、read-only では `VIEW` badge、read-only banner、edit controls 非表示、push/autosave/edit key blocking を行う。
- VM 側は Guest Additions が ready になったため、`scripts/ops/vm_team_ws_test.bat warm` を `guestcontrol start + report polling` 方式へ修正して PASS。VM 内 report: Setup PASS / Launch PASS / Smoke PASS。
- 検証: `npm --prefix beta run build` pass。`npx playwright test --config playwright.team-vm.config.js --retries 0 --workers 1` pass（3 tests: journey, host/vm conflict, view/editor split）。
- Swingby 定例会用に `map_team_swingby_monthly_2604` を `ws_team_swingby` へ取り込み、元バックアップの `2604` scope を `4月` 表示へ正規化した。
- `scripts/internal/launch-swingby-server.bat` を実体化し、Tailscale Funnel 公開先 `localhost:4173` を Swingby 定例会 map で起動できるようにした。
- VM から公開 URL を検証する `scripts/ops/vm_public_meeting_link_test.{bat,ps1}` を追加。VM 内から `viewer.html` / map API へ到達し、root `定例会`、`4月` scope、84 nodes を確認した。

## Swingby same-workspace conflict lab

- `ws_team_swingby` 内に、定例会 map とは別の検証用 map `map_team_swingby_conflict_lab_260502` を使う conflict lab runner を追加した。
- `scripts/ops/vm_swingby_conflict_lab.{ps1,bat}` は host が seed/newer edit、VM が public URL から stale read/stale save、409 検出、明示 resolution force save、host final verify までを一括実行する。
- Akaghef 向け runbook `docs/for-akaghef/260502_swingby_conflict_lab.md` に public viewer/API link、実行コマンド、pass 条件、blocker を整理した。
- 検証: `scripts\ops\vm_swingby_conflict_lab.bat` pass。public URL と VM guestcontrol 経由で 409 stale conflict と force resolution を確認し、final state に host / VM resolution の両方が残ることを確認した。

## V4 service-equivalent vertical slice

- V4 を Mapify / Miro / Obsidian / GitHub の外部連携ではなく、M3E 内部へ取り込む service-equivalent design として整理した。
- `docs/03_Spec/V4_Service_Equivalent_Design.md` を追加。Mapify は Flash の主要機能、Miro は surface / discussion / decision node、Obsidian は local-first vault sync、GitHub は always staged proposal / secret lock / Akaghef PC 正本同期として定義した。
- `scripts/ops/v4_service_slice_demo.{mjs,bat}` を追加。PDF 抽出、Flash ingest/approve、Miro-style discussion node、Obsidian vault export/edit/import、409 conflict、explicit resolution、M3E screenshot、2-pane report HTML 生成を一括実行する。
- 成果物: `docs/for-akaghef/v4_demo/260502_v4_service_slice_report.html`, `docs/for-akaghef/v4_demo/v4_service_slice_summary.json`, `docs/for-akaghef/v4_demo/assets/v4_service_slice_m3e.png`, `docs/for-akaghef/v4_demo/obsidian_vault/`。
- 検証: `scripts\ops\v4_service_slice_demo.bat --publicBase https://akaghef-dell.tail6206ae.ts.net` pass。`map_team_swingby_v4_slice_260502` で Flash committed nodes 7件、vault export/import complete、stale write HTTP 409、resolution 後に host decision と VM resolution が両方残ることを確認した。Obsidian import 後も final map に Flash / Miro / Obsidian / GitHub anchor が残るよう正規化し、VirtualBox `M3E-Test` から public URL 経由で root / Flash anchor / GitHub anchor / VM resolution を確認した。
