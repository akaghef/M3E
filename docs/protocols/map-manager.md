# Map Manager Protocol

This file is a compatibility entry point.

The operational SSOT is the package:

```text
docs/protocols/map-manager/
```

Start with:

- `docs/protocols/map-manager/README.md`
- `docs/protocols/map-manager/gates.md`
- `docs/protocols/map-manager/projection-rule.md`

Do not add new Map Manager rules to this wrapper. Update the package and the
machine-checkable contracts under `docs/protocols/contracts/` instead.
